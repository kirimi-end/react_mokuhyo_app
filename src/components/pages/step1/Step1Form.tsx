import { TextField } from "@mui/material"

const Step1Form = () => {
  return (
    <TextField
      fullWidth
      label="お名前"
      sx={{
        backgroundColor: "white",
      }}
    />
  )
}

export default Step1Form
