import { Box } from "@mui/system"

const TimerStart = () => {
  return (
    <Box
      sx={{
        color: "#1661D1",
        fontWeight: "bold",
        fontSize: "40px",
        textAlign: "center",
      }}
    >
      START!
    </Box>
  )
}

export default TimerStart
